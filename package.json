{
  "name": "my_ram",
  "private": true,
  "version": "1.1.4",
  "type": "module",
  "scripts": {
    "tauri": "tauri",
    "build": "tauri build & ls src-tauri/target/release/bundle/msi/Myram_*.zip | tail -n 1 | xargs -i $1 mv $1 ./releases/"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^1.2.2"
  },
  "dependencies": {
    "sortablejs": "^1.15.0"
  }
}